[package]
name = "toybox-server"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = "0.16"
bevy_scriptum = { version = "0.9", features = ["lua"] }
bevy-inspector-egui = "=0.33.1"
bevy_egui = "0.36.0"

shared = { path = "../shared" }
